# Local / Heroku-style: run from `backend/` with repo root on PYTHONPATH (sibling `backend` package).
web: sh -c 'export PYTHONPATH=.. && uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}'
